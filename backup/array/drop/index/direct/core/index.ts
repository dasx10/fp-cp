import type { ToIndexDirect } from "../../../../../arrayLike/index/index.D";

const dropDirectCore = <Index extends number, X extends readonly any[]>(index: ToIndexDirect<Index>, x: X) => {
	const { length } = x;
	if (length > 0) {
		
	}

	return [];
}

export default dropDirectCore;
