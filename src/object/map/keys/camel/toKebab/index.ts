// function camelKeysToKebab (record) {
//     const mapped = Object.create(record);
//     // for (const key in record) mapped[//WIP] = record[key];
//     // return mapped;
// }

// function CamelToKebabKeys <T extends Record<PropertyKey, any>>(record: T) {
//   const mapped = Object.create(record);
//   for (const key in record) mapped[//WIP] = record[key];
// }
